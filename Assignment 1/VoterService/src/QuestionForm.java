/**
 * QuestionForm is an enumerated class that defines if the question uses:
 * A, B, C, D (LETTERS) or RIGHT AND WRONG (RIGHT_WRONG) for the answer choice
 */
public enum QuestionForm {
    RIGHT_WRONG,
    LETTERS
}
